source "http://rubygems.org"

# Specify your gem's dependencies in adapter_generator.gemspec
gemspec

group :development do
  # Override gemspec to default fakefs to safe mode
  gem 'fakefs', :require => 'fakefs/safe'
end
